# One entry of the `servers` list, applied to every entry of it: each port of
# this program starts from the same baseline and overrides what differs.
servers:
  - method: 2022-blake3-aes-256-gcm
    mode: tcp_and_udp
    timeout: 300
