# The client's own baseline: the method has to agree with the server's, and
# the local listener is the client's business alone.
servers:
  - method: 2022-blake3-aes-256-gcm
    mode: tcp_and_udp

local_address: 127.0.0.1
local_port: 1080
