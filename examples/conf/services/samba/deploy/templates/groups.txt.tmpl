# name:gid — the group Samba writes as. The container starts with no such
# group, so its entrypoint creates this one before smbd starts.
{{- $accounts := (instance).values.accounts }}
{{ $accounts.group.name }}:{{ $accounts.group.gid }}
