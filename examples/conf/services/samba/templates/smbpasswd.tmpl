{{- /*
  Samba's account table. One line per credential granted on the smb port,
  holding the NT hash of that credential and never the credential.

  The uid is the person's POSIX account, so two of someone's credentials are
  two entries pointing at one account. `usermap` beside this file is what
  resolves the name.
*/ -}}
{{ range $p := principals "smb" }}{{ smbpasswd $p.Name (required (index $.accounts.uids $p.User) (printf "accounts.uids: %s" $p.User)) $p.Secret }}
{{ end }}